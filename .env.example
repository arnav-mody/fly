# Copy this to .env and fill in real values. Never commit the real .env.

ANTHROPIC_API_KEY=

SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=

# Personal access token from supabase.com/dashboard/account/tokens — lets the
# CLI deploy Edge Functions / run migrations without a per-session login.
SUPABASE_ACCESS_TOKEN=
